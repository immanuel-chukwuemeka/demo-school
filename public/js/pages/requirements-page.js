/* Bootstrap for the full-size requirement checker page. */
import { initRequirementWidget } from "./requirements.js";
import { initReveal } from "../lib/anim.js";

initRequirementWidget();
initReveal();
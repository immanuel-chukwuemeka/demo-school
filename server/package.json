{
  "name": "greenwood-seed",
  "version": "1.0.0",
  "private": true,
  "description": "Offline seeding for Greenwood Academy — Firestore data + Firebase Auth users.",
  "type": "commonjs",
  "main": "seed.js",
  "scripts": {
    "seed": "node seed.js",
    "seed:noauth": "node seed.js --no-auth"
  },
  "dependencies": {
    "firebase-admin": "^12.1.0"
  },
  "engines": {
    "node": ">=18"
  }
}